How to compile and run the program?

1. Ensure the makefile is in the same directory as the system files.

2. The code can only run in a linux environment.

3. In the terminal run the command "make run".

4. Have fun with the program.